import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
	palette: {
		primary: {
			main: '#f1c40f',
			light: '#F2E00F',
			dark: '#BFA332',
			contrastText: '#fff',
		},
		secondary: {
			main: '#0044ff',
		},
	},
})
